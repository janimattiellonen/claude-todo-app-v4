import * as stylex from '@stylexjs/stylex'

// Test color variables using static hex values
export const colors = stylex.defineVars({
  primary: '#0070f3',
  secondary: '#7928ca',
  background: '#ffffff',
  text: '#000000',
})
